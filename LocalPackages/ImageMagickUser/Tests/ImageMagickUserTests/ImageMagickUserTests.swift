    import XCTest
    @testable import ImageMagickUser

    final class ImageMagickUserTests: XCTestCase {
        func testExample() {
            // This is an example of a functional test case.
            // Use XCTAssert and related functions to verify your tests produce the correct
            // results.
            XCTAssertEqual(ImageMagickUser().text, "Hello, World!")
        }
    }
